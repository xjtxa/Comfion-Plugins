# Comfion Plugins

Official downloadable plugin catalog for Comfion. The application reads `catalog.json`, downloads a versioned release asset, verifies its SHA-256 digest, and installs it locally.

Plugin packages execute trusted local code. Install packages only through Comfion or after verifying their release source.
